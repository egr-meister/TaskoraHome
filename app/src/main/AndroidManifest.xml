<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      Taskora Home requests NO permissions. The app is fully offline and stores
      all data locally via DataStore. Do not add INTERNET, CAMERA, LOCATION,
      NOTIFICATIONS, STORAGE, CALENDAR, BLUETOOTH, NFC, or ALARM permissions.
    -->

    <application
        android:name=".TaskoraApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TaskoraHome">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:configChanges="uiMode|fontScale|smallestScreenSize|screenSize|screenLayout|keyboardHidden"
            android:theme="@style/Theme.TaskoraHome.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
